<!DOCTYPE html>
<html>
    <head>
        <title>MakersBnB - Requests</title>
        <link rel="stylesheet" href="/style-view-requests.css"
        <link rel="icon" href="/MakersBnB Logo Just M.svg">
    </head>
    <nav>
      <div>
        <a href="/">
          <img class="resize" src="/logo.png" />
        </a>
      </div>
      <div class="nav_links_container">
      <div class="nav_links">
      <a href="/account" class="button-link">Account Page</a>
      <a href="/logout" class="button-link">Log out</a>
      </div>
      </div>
    </nav>
    <body>
      <div class="container">
        <h2>These are the requests for: <%= @listing.listing_name %></h2>
        <br>
        <% if @requests %>
        <h2>View your current requests:</h2>
        <% @requests.each do |request| %>
            <div class="request">
              <div class="request-header">
                <h3>Request:</h3>
                <p>Booking User: <%= request.booking_user_name %></p><br />
              </div>
              <div class="request-details">
                <p>Date: <%= request.date %></p><br />
                <form method='POST' action='/confirm'>
                  <input type="hidden" name="user_id" value="<%= request.booking_user_id %>"><br>
                  <input type="hidden" name="date_id" value="<%= request.date_id %>"<br>
                  <button>Confirm Booking</button>
                </form>
              </div>
            </div>
        <% end %>
        <% else %>
        <div class="container">
          <h2> Your listing currently has no requests </h2>
        </div>
        <% end %>
    </body>
</html>