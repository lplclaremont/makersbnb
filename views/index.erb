<!DOCTYPE html>
<html>
    <head>
        <title>MakersBnB</title>

        <!--
            Head to https://github.com/makersacademy/web-applications/blob/main/pills/loading_css_or_static_files.md
            If you're wondering how the CSS file below is being loaded.
        -->
        <link rel="stylesheet" href="/style.css" >
    </head>
    <body>
        <h1>Makersbnb</h1>

        <% if @session_id.nil? %>
            <div>
                <a href="/signup">Sign up</a>
                <a href="/login">Log in</a>
            </div>
        <% else %>
            <div>
                <a href="/logout">Log out</a>
            </div>
        <% end %>

        <h2>View current properties:</h2>
        <% @listings.each do |listing| %>
            <div>
            Property name: <%= listing.listing_name %><br />
            Price per night: £<%= listing.price %><br />
            Hosted by: <%= listing.host_name %><br />
            View full details <a href="/listings/<%= listing.id %>">here</a>
            </div>
            <br />
            <p>------</p>
        <% end %>
    </body>
</html>
